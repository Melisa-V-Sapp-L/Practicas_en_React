import React from 'react';
import ReactDOM from 'react-dom/client';

import ColorPickertp from './ToChange';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <ColorPickertp />
  </React.StrictMode>
);

